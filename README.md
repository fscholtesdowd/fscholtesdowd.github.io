# fscholtesdowd.github.io

The front door. Hub page + root `robots.txt` + sitemap index for every site on this domain.

Live at: https://fscholtesdowd.github.io/

**Vault note (agents):** local source lives at `02 - Projects/Web Properties/fscholtesdowd.github.io/`. When a new site ships, add it in THREE places: a card in `index.html`, an entry in `sitemap.xml` (if the site has its own sitemap) or `sitemap-root.xml` (if not), and then push. Created 2026-08-24 on Forrest's go-ahead — fixes the 404 front door found by the Search Discovery Audit.
